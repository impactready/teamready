<div class="spacer">
</div>
<div class="heading-block">
  <% if current_user.master_user? %>
      <h2>All users</h2>
  <% else %>
      <h2>All users in your teams</h2>
  <% end %>
</div>
<% if @users.count == 0%>

  <% if current_user.master_user? %>
    <p>You have not added any users to this account.</p>
  <% else %>
    <p>You have no fellow members. You need to be assigned to teams to see your fellow members.</p>
  <% end %>

<% else %>

  <div class="spacer">
  </div>

  <div class="item-table">
    <table>
      <tr class="listing-header">
        <td>Name:</td>
        <td class="listing-i-fxl">Team memberships:</td>
        <td></td>
      </tr>
      <%= render @users %>
    </table>
  </div>

<% end %>